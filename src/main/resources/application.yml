server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://mysql_db:3306/book?useSSL=false&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
